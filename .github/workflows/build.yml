name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Unzip Project Files
        run: |
          unzip -o YouTube-Music-Stream.zip -d extracted_app
          ls -la extracted_app

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Webview APK with Capacitor
        run: |
          npm install -g @capacitor/cli @capacitor/core
          mkdir android_project && cd android_project
          npx cap init "YMusic" "com.ymusic.app" --web-dir ../extracted_app
          npm install @capacitor/android
          npx cap add android
          npx cap sync
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: YMusic-App
          path: android_project/android/app/build/outputs/apk/debug/app-debug.apk
